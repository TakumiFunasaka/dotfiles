# ============================================================================
# Modern tmux Configuration (2025)
# ============================================================================

# ----------------------------------------------------------------------------
# 基本設定
# ----------------------------------------------------------------------------
# prefixキーを Ctrl+a に変更（最もメジャーな代替キー、GNU screen由来）
# Ctrl+a を2回押すと、シェルの「行頭移動」として送れる
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# 256色 + True Color サポート
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# マウス操作を有効化（ペイン選択・リサイズ・スクロール）
set -g mouse on

# ウィンドウ・ペイン番号を1から始める（0は遠い）
set -g base-index 1
setw -g pane-base-index 1

# ウィンドウを閉じたとき番号を詰める
set -g renumber-windows on

# エスケープキーの遅延をなくす（vimユーザー向け）
set -sg escape-time 0

# ヒストリを大きく
set -g history-limit 50000

# フォーカスイベント（vim連携用）
set -g focus-events on

# ----------------------------------------------------------------------------
# キーバインド - ペイン操作
# ----------------------------------------------------------------------------
# ペイン分割（直感的なキー + 現在のディレクトリを引き継ぐ）
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# ペイン移動（vim風: h/j/k/l）
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# ペインリサイズ（Shift + vim風キー）
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# ----------------------------------------------------------------------------
# キーバインド - ウィンドウ操作
# ----------------------------------------------------------------------------
# 新しいウィンドウ（現在のディレクトリで）
bind c new-window -c "#{pane_current_path}"

# ウィンドウ切替
bind -r n next-window
bind -r p previous-window

# ----------------------------------------------------------------------------
# キーバインド - その他
# ----------------------------------------------------------------------------
# 設定リロード
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"

# コピーモードでvim風キーバインド
setw -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"

# ----------------------------------------------------------------------------
# 見た目 - ステータスバー
# ----------------------------------------------------------------------------
# ステータスバーの位置（下）
set -g status-position bottom

# 更新間隔
set -g status-interval 5

# 色設定 (Gruvbox Dark)
set -g status-style "bg=#282828,fg=#ebdbb2"

# 左側: セッション名
set -g status-left-length 30
set -g status-left "#[fg=#282828,bg=#fabd2f,bold]  #S #[fg=#fabd2f,bg=#282828] "

# 右側: 日時
set -g status-right-length 50
set -g status-right "#[fg=#a89984] %m/%d %H:%M "

# ウィンドウリスト
set -g window-status-format "#[fg=#a89984]  #I:#W "
set -g window-status-current-format "#[fg=#282828,bg=#b8bb26,bold]  #I:#W #[fg=#b8bb26,bg=#282828]"

# ペインの境界線
set -g pane-border-style "fg=#504945"
set -g pane-active-border-style "fg=#fabd2f"

# メッセージ
set -g message-style "bg=#fabd2f,fg=#282828"

# ----------------------------------------------------------------------------
# セッション用レイアウト (prefix + D で開発レイアウト)
# ----------------------------------------------------------------------------
bind D source-file ~/.tmux/dev-layout.conf

# vim:set ft=tmux:
